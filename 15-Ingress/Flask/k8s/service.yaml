apiVersion: v1
kind: Service
metadata:
  name: hello-world
spec:
  selector:
    app.kubernetes.io/name: hello-world # Must match the label on your Pods
  ports:
    - protocol: TCP
      port: 80 # Port exposed by the Service
      targetPort: 80 # Port the container listens on
